<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.Safar"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity
            android:name=".Screen1"
            android:exported="true">
            <!-- Intent filter or other configurations if needed -->
        </activity>


        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <!-- Intent filter or other configurations if needed -->
        </activity>


        <activity
            android:name=".ForgetPassword"
            android:exported="true">
            <!-- Intent filter or other configurations if needed -->
        </activity>

        <activity
            android:name=".RefundActivity"
            android:exported="true">
            <!-- Intent filter or other configurations if needed -->
        </activity>

        <activity
            android:name=".HomeActivity"
            android:exported="true">
            <!-- Intent filter or other configurations if needed -->
        </activity>


        <activity
            android:name=".ArrivalScreenActivity"
            android:exported="true">
            <!-- Intent filter or other configurations if needed -->
        </activity>

        <activity
            android:name=".CalendarActivity"
            android:exported="true">
            <!-- Intent filter or other configurations if needed -->
        </activity>

        <activity
            android:name=".DepartureArrivalActivity"
            android:exported="true">
            <!-- Intent filter or other configurations if needed -->
        </activity>

        <activity
            android:name=".SignUp"
            android:exported="true">
            <!-- Intent filter or other configurations if needed -->
        </activity>

        <activity
            android:name=".BuyPreBookedActivity"
            android:exported="true">
            <!-- Intent filter or other configurations if needed -->
        </activity>

        <activity
            android:name=".PreBookedActivity"
            android:exported="true">
            <!-- Intent filter or other configurations if needed -->
        </activity>

        <activity
            android:name=".SelectSeatActivity"
            android:exported="true">
            <!-- Intent filter or other configurations if needed -->
        </activity>

        <activity
            android:name=".Selectseatnotify"
            android:exported="true">
            <!-- Intent filter or other configurations if needed -->
        </activity>



        <activity
            android:name=".CustomerSupportActivity"
            android:exported="true">
            <!-- Intent filter or other configurations if needed -->
        </activity>

        <activity
            android:name=".Feedback"
            android:exported="true">
            <!-- Intent filter or other configurations if needed -->
        </activity>

        <activity
            android:name=".PopularRoutesActivity"
            android:exported="true">
            <!-- Intent filter or other configurations if needed -->
        </activity>

        <service
            android:name=".MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>



    </application>

</manifest>