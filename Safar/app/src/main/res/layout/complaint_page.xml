<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <!-- Back button -->
    <ImageView
        android:id="@+id/backButton"
        android:layout_width="44dp"
        android:layout_height="43dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:contentDescription="Back"
        android:padding="8dp"
        android:src="@drawable/back_arrow_background" />

    <!-- Complaint heading -->
    <TextView
        android:id="@+id/complaintHeading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Complaint"
        android:textStyle="bold"
        android:textSize="24sp"
        android:textColor="#101a5b"
        android:layout_below="@id/backButton"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp" />

    <!-- Complaint message -->

    <!-- Submit button -->
    <EditText
        android:id="@+id/complaintMessage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/complaintHeading"
        android:layout_marginTop="208dp"
        android:background="@drawable/otp_boxes"
        android:hint="Enter your complaint here"
        android:inputType="textMultiLine"
        android:minLines="6" />

    <Button
        android:id="@+id/submitButton"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/complaintMessage"
        android:layout_marginStart="60dp"
        android:layout_marginTop="56dp"
        android:background="@drawable/rounded_button"
        android:text="Submit" />

</RelativeLayout>
